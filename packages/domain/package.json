{
  "name": "@nutriscan/domain",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@nutriscan/types": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
